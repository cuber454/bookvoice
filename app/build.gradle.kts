plugins {
    id("com.android.application")
}

android {
    namespace = "com.cuber.bookvoice"
    compileSdk = 36

    defaultConfig {
        applicationId = "com.cuber.bookvoice"
        minSdk = 24
        targetSdk = 36
        // Ветка portyanka: сплошная прокрутка книги (портянка, msg4308).
        // Номер выше 0.4.26, чтобы в «О программе» было видно, какая сборка
        // стоит на телефоне. Соберём релизом — только по слову Сергея.
        // msg4416: 0.4.27 успела разойтись несколькими тестовыми сборками, и в
        // «О программе» их было не различить — склейка предложений помечена 0.4.28.
        // 0.4.31: убрана галочка «Прыгать на ходу, не дожидаясь остановки»
        // (msg4450/4452) — прыжок остался только по отпусканию прокрутки.
        // 0.4.32: пункт «Сетевые библиотеки» в меню открытой книги (msg4464).
        // 0.4.33: очередь заготовок звука (msg4472) + строка «Работа в фоне»
        // в корне настроек (msg4476).
        // 0.4.34: «Выход» как действие сессии — кнопка в медиа-карточке
        // Android 13+ (msg4578) + тестовая галочка «Бесшовная передача звука»
        // (msg4598; по умолчанию выкл, сравниваем стыки на слух).
        // 0.4.35: встык вариантом B со сверкой конвейера (a74535f, msg4685),
        // карточка живёт на паузе (9831dd4), вторая строка карточки — глава
        // (b3d79df), экран «Не засыпать» (5f9125b) и тихий звуковой поток
        // галочкой (1609525), форматы сетевых библиотек (79abca0), «⋮» с
        // настройками во всех окнах (227cd37), процент на ползунке (e945dd2),
        // «Настройки» перед «Выходом» во всех меню (7f6872c).
        // 0.4.39: у «главы» гарнитуры три уровня вместо отдельной строки (538319e,
        // msg5295), сторож молчания — запал 6/1 с и пауза не обнуляет лестницу
        // (60b926c, msg5309), раздел «Интерфейс» → «Экран книги», вкладки полки
        // переехали в «Библиотеку» (2f90851, msg5317).
        // 0.4.40: голоса по языкам — «Движок / Язык / Голос» вместо плоского
        // списка (ba83116, msg5503/5511), нижняя панель читалки — значок с
        // подписью (df85a75, msg5561), нечитаемый файл говорит причину и уходит
        // назад (ee0672f, msg5475), автор в лентах каталога больше не берётся у
        // последней записи (64d1746), альтернативный способ озвучки (100af6a),
        // волшебное касание после выхода из окна (642f1af).
        // 0.4.41: стык у разных движков — глубина заготовок по времени и сверка
        // конвейера (a4e341f, msg5585), смена голоса/движка на ходу перечитывает
        // текущее предложение и «Пауза между фразами» (9e4794b, msg5600/5604),
        // выбранный язык переживает смену движка (c24af26, msg5622), единый набор
        // «Голос чтения» на читалку и Настройки (61c40ae/293f6c1, msg5610), нарезка
        // Настроек на подразделы и заголовки (10e52d1, d7d8244, 40c5a35, msg5679+).
        // 0.4.42: голос помнится по движкам — в строке «Голос» больше не остаётся
        // имя голоса прошлого движка (cc02e14, msg5722/5726), плюс окно «Что нового»
        // после обновления (1c86759, #18) с текстами выпусков 0.4.26–0.4.42.
        // Визуальные три коммита (d8820a8/da0e5d8/d6ea1a3) в эту версию НЕ входят —
        // Сергей отложил их на отдельный выпуск (msg5768).
        // 0.4.43: «Удалить всё из категории» (16a032e, msg5947), «Останавливать
        // чтение» (c0c1b2b), «Системные кнопки» (713c997), бесшовная передача
        // без галочки (512991d, msg5955), потолок скорости 6.0 + заказанная
        // скорость в журнале (ad79571, msg5979), FB2 из чужих менеджеров
        // (1b0d1de), карточка в шторке (4329cd2), падение после «книга не
        // открылась» (fe554f5). Визуалы, отложенные с 0.4.42, входят здесь —
        // Сергей сказал «да» (msg5967).
        // 0.4.44: синхронизация между устройствами — облачная папка (beba225),
        // вход в Яндекс.Диск (ac1a705), перенос книг (b4b8d90), удаление по
        // метке (da5b00e); сортировка полки по сериям (3d61346); фикс открытия
        // чужого файла (e738fdf, msg6148).
        // 0.4.45: падение по кругу на 0.4.44 (msg6161) — усечённый архив в
        // разборе метаданных полки ронял процесс (5616cda).
        // 0.4.50: связка абзаца с диктором — пробросы наведения/клавиш/фокуса
        // в ExploreByTouchHelper (msg6338: TalkBack не читал абзацы вовсе, у
        // Jieshuo читались), в diag.log пишем включённых дикторов и обращения
        // к узлам предложений; выключка по ширине и переносы (msg6340/6342).
        // 0.4.51: узел предложения объявлен удерживаемым (isLongClickable) —
        // без этого «двойной тап с удержанием» не начинал выделение в цитаты
        // (msg6348); обычное касание без диктора снова попадает в предложение
        // под пальцем.
        // 0.4.52: именованные действия узла предложения («Читать отсюда»,
        // «Выделить фрагмент отсюда») — видны в меню «Действия» диктора;
        // в diag.log пишем приходящие действия и настоящее касание (msg6352/6354).
        // 0.4.53: обработчики жеста ставятся всегда, в том числе при включённом
        // обходе касанием (msg6364: лог показал, что настоящее касание до текста
        // доходит, а действий диктор не шлёт вовсе — прежняя защита оставляла
        // такого пользователя без жестов); защита от двойного срабатывания
        // жест/действие и метки «нажатие»/«удержание» в diag.log.
        // 0.4.54: убран свой сигнал «говори» перед голосовым поиском (msg6404) —
        // распознаватель подаёт собственный, вместе звучали как два подряд.
        // 0.4.55: переход по главе больше не откатывается назад (msg6416) — своя
        // прокрутка ленты отличается от читательской (scrollRequestRow), прежняя
        // «цель руки» снимается на переходе, отложенная проверка паузы не срабатывает,
        // если чтение уже пошло; в журнал — назначение свайпа и цель с ленты.
        // 0.4.56: выпуск после 0.4.42 — публичного релиза между ними не было,
        // поэтому «Что нового» идёт ОДНИМ блоком 116: прежний блок 103 объявлял
        // «Останавливать чтение», удалённую коммитом 1e6306c (msg6022), а тексты
        // в этом окне читают вслух. Правило Сергея (msg6453): в списке только
        // крупное, мелочи не пишем. Собранное: синхронизация устройств, абзацы
        // с узлами предложений, голосовой поиск, оглавление на главе, категория
        // целиком и серии, скорость 6.0 и бесшовный стык, визуальные настройки,
        // устойчивость к битому файлу.
        // 0.4.57: обложки в карточках-сетке (просьба Сергея от 23.09.2026:
        // «хочу, чтобы показывалось как в FBReader») — новый BookCovers достаёт
        // картинку из FB2 (потоково, из <binary>), из EPUB (по описи, включая
        // обложку-страницу) и из книги внутри архива (сначала FB2, потом EPUB —
        // как и разбор книги), кладёт байты в filesDir/covers и декодированный
        // битмап в память; книга без обложки получает нарисованную из названия.
        // Для диктора ничего не изменилось: картинка помечена как неважная,
        // узел карточки один, текст тот же (название, автор, процент).
        // Там же галочка «Показывать обложки книг» (раздел «Библиотека и
        // скачанные», по умолчанию включена): снятая возвращает карточку к
        // прежнему виду и не даёт открывать книги ради картинок. Кэш обложек —
        // часть ОБЩЕЙ строки «Кэш» в настройках (одна настройка, как просил
        // Сергей), обложка снимается при удалении книги, а замену файла книги
        // по тому же адресу ловит записанный рядом размер оригинала.
        // Плюс конец книги: дочитав последнее предложение, движок через секунду
        // говорит голосом чтения «Книга прочитана» (просьба Сергея), а любое
        // действие читателя эту фразу отменяет. Сама книга при этом помечается
        // прочитанной на полке.
        // 0.4.58 (код 118): срочная правка к 0.4.57 — приложение не запускалось.
        // Страница библиотеки создаётся в конструкторе окна
        // (`private val library = LibraryActivity(this)`), а там у Activity ещё нет
        // базового контекста: книга обложек, созданная обычным полем, читала
        // act.applicationContext и падала с NullPointerException ещё до первого
        // кадра (стек: LibraryActivity.<init> ← LibraryWindowActivity.<init>).
        // Обложки и адаптер полки теперь создаются лениво, при первом обращении.
        // Урок — в docs/PROJECT.md, раздел 12.
        // 0.4.59 (код 119): карточка сетки — без автора и с четырьмя строками
        // названия (решение Сергея 23.09.2026): строка автора отдана названию,
        // длинные названия почти перестают обрезаться. Диктор автор по-прежнему
        // слышит — он приходит текстом карточки, а не из разметки; в списке автор
        // тоже остался. При крупном шрифте (слой для слабовидящих) сетка
        // становится в одну колонку: крупная обложка и читаемое название важнее
        // плотности полки.
        // 0.4.60 (код 120): 1) автор на карточке сетки показывается ТОЛЬКО без
        // обложки (снята галочка или картинки нет) — тогда карточка снова
        // текстовая, и автор в ней нужен; с обложкой строку отдали названию.
        // 2) Новая галочка в «Чтении»: «Останавливать чтение, когда телефон
        // перевёрнут экраном вниз» (по умолчанию снята) — экран погашен и
        // телефон лёг экраном вниз, чтение встаёт на паузу; перевернули экраном
        // вверх, чтение продолжается само (FaceDownPause). Пауза временная: она
        // не снимает таймер сна, а пока таймер ждёт свой жест, переворот
        // принадлежит ему — настройки не пересекаются.
        // 0.4.61 (код 121): обложка для PDF. В файле картинки-обложки нет, её
        // рисуем из первой страницы (pdfbox-ом, он уже в проекте для текста PDF),
        // сразу в размер карточки и в JPEG; пустая (почти белая) первая страница
        // обложкой не считается — вместо неё нарисованная заглушка с названием.
        // Так же поступает FBReader — Сергей сверил на одних и тех же книгах.
        // 0.4.62 (код 122): правило «пустой страницы» для PDF переделано. У
        // Сергея PDF с текстовым слоем, то есть первая страница — титул с парой
        // строк, и первый порог (97% фона) считал такую страницу пустой, подменяя
        // её заглушкой. Теперь пустым считается только ровный СВЕТЛЫЙ лист без
        // «чернил» (≥99,85% фона и яркость фона ≥0,85); титул показывается, и
        // однотонная тёмная обложка тоже.
        // 0.4.63 (код 123): обложку PDF рисует СИСТЕМНЫЙ отрисовщик Android
        // (PdfRenderer, внутри PDFium), а pdfbox остался запасным путём. Причина:
        // Сергей прислал «Человека и мир» (5 класс) — обложка там картинка в
        // JPEG 2000, а в pdfbox-android такого декодера нет вовсе, страница
        // отрисовывалась пустой и книга получала нарисованную заглушку. Проверено
        // у себя: с декодером JPEG 2000 та же страница даёт нормальную обложку.
        // 0.4.64 (код 124): архив определяем по СОДЕРЖИМОМУ, а не по имени файла.
        // Сергей прислал «Карточные игры России.fb2» — это zip с книгой внутри;
        // по имени мы читали архив как XML, обложки не находили. Теперь первые
        // четыре байта решают (как в BookParser.isZip), и такие книги получают
        // обложку. Заодно так же лечатся epub и fb2 в архиве под любым именем.
        // 0.4.65 (код 125): карточка сетки — только обложка. Подписи под ней
        // (название и автор) убраны: они повторяют то, что и так написано на
        // обложке; без обложек подписи возвращаются. В углу обложки — цифра
        // прочитанного у начатых книг (только для глаз: диктору процент приходит
        // текстом карточки). Полоска прогресса не возвращается — её владелец
        // запрещал, а цифра это не полоска, и своей галочки у неё нет.
        // 0.4.66 (код 126): надзор за нашим плеером. Движок — не единственный,
        // кто может замолчать: бывает, файл отдан плееру, а звука нет. Раз в две
        // секунды сверяем позицию играющего файла; стоит шесть секунд — фраза
        // считается доигранной. Так же ловится потерянный onCompletion (раньше
        // это было молчанием навсегда). Судим только после того, как видели рост
        // позиции, и закрываем фразу ровно один раз.
        // 0.4.67 (код 127): сроки сторожей укорочены, и не одним числом, а по длине
        // фразы. Просьба Сергея: «давай укорачиваем» — в случае «процесс жив, а
        // звука нет» он выбрал короткие сроки вместо сбора замеров. Синтез: ждём
        // четверть времени звучания фразы, от 2,5 до 6 с (было 6 с всегда);
        // переспрос остался прежним — 1 с, значит перезапуск движка теперь через
        // 3,5–4,7 с вместо 7 (на самой медленной скорости потолок в 6 с возвращает
        // прежние 7 с, но там и речь вдвое длиннее). Подготовка своего файла: 5 с
        // вместо 12. Позиция играющего файла: 4 с вместо 6. Прямая речь: срок
        // считается по длине фразы (было 180 с на всё).
        // Тем же выпуском: в альтернативной озвучке движку отдаём три фразы
        // вперёд (очередь вместо «одна фраза — ждём конец»), чтобы он читал без
        // пауз. Догадка Сергея; сторож в этом режиме теперь один — на первую
        // фразу очереди, а дальше его ведёт сигнал «начал говорить».
        versionCode = 127
        versionName = "0.4.67"
    }

    signingConfigs {
        // CI (#35): APK автообновления обязан быть подписан тем же ключом, что и
        // установленная версия, иначе апдейт не встанет поверх. На GitHub-раннере
        // AGP ищет debug-keystore не в $HOME/.android, а где-то ещё, и молча
        // генерирует новый — поэтому на CI путь к ключу задаём явно через env.
        // Локально env нет — остаётся стандартный ~/.android/debug.keystore.
        val ciKs = System.getenv("BV_DEBUG_KEYSTORE")
        if (ciKs != null) {
            create("ciDebug") {
                storeFile = file(ciKs)
                storePassword = System.getenv("BV_DEBUG_KEYSTORE_PW") ?: "android"
                keyAlias = "androiddebugkey"
                keyPassword = System.getenv("BV_DEBUG_KEY_PW") ?: "android"
            }
        }
    }

    buildTypes {
        debug {
            if (System.getenv("BV_DEBUG_KEYSTORE") != null) {
                signingConfig = signingConfigs.getByName("ciDebug")
            }
        }
        release {
            isMinifyEnabled = false
        }
    }
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    // AGP 9 built-in Kotlin: jvmTarget берётся из compileOptions.targetCompatibility.
    buildFeatures {
        viewBinding = true
    }
}

dependencies {
    implementation("com.squareup.okhttp3:okhttp:4.12.0")
    implementation("androidx.core:core-ktx:1.13.1")
    // enableEdgeToEdge для edge-to-edge (SDK 36): appcompat тянет activity 1.7.0,
    // где этой функции ещё нет — поднимаем до версии с ней.
    implementation("androidx.activity:activity:1.9.3")
    implementation("androidx.appcompat:appcompat:1.7.0")
    implementation("com.google.android.material:material:1.12.0")
    implementation("androidx.media:media:1.7.0")
    // ExploreByTouchHelper (ParagraphView, msg6322): предложения внутри абзаца —
    // виртуальные узлы для диктора. Тянется и транзитивно (recyclerview →
    // customview), но зависимость эта явная — держим её в списке.
    implementation("androidx.customview:customview:1.1.0")
    // PDF: извлечение текстового слоя + дерево закладок (для PDF-инструкций).
    implementation("com.tom-roush:pdfbox-android:2.0.27.0")
}
