# Сборка
.gradle/
build/
app/build/
.kotlin/

# Локальная машина: путь к SDK не общий
local.properties

# Готовые APK в git не храним — их собирают GitHub Actions и кладут в Releases
*.apk
dist/

# Логи и временное
*.log
.idea/
*.iml

# Черновики прогонов (логи дымовой компиляции и прочее) — в git не нужны
scratch/

# Локальный кэш Gradle: GRADLE_USER_HOME держим внутри проекта
.gradle-home/
